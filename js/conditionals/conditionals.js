// This code statement always runs
// regardless of whether the condition returns true or false

if(condition){
    
}

// You may sometimes see if...else statements written without curly braces

if(condition) // code to run if condition is true
else {} // run some other code instead

// It's much easier to understand the code if you use curly braces to delimit
// the blocks of code
//
// Por lo general se elije if else para condiciones complejas :
